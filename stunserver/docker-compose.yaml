version: '3'
services: 
    server:
      container_name: stunserver
      environment:
        - UDPPORT=80
      build: .
      ports:
      - "8080:80"
      - "8080:80/udp"
    test:
      container_name: tests
      build: 
        context: .
        dockerfile: ./tests/Dockerfile
